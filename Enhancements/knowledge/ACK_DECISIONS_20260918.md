# Decision record — conflicts between recent changes and the governing specification

18 September 2026 · prepared by Claude Code for ACK · **status: D1 and D2 DECIDED (ACK, 18 Sep 2026); D3–D9 FOR ACK DECISION**

The authority order (CLAUDE.md) is: business decisions (ACK) → specification v1.1 → method notes → design documents →
code. Work on 17–18 September introduced rules that contradict the specification. Each item below states the
specification's position, what has been built or approved since, the evidence, the options and a recommendation.
Nothing here takes effect until ACK records a decision in the last column; once decided, the specification is
amended by reference to this record.

| # | Topic | Specification position | Built or approved since | Evidence | Options | Recommendation | ACK decision |
|---|---|---|---|---|---|---|---|
| **D1** | **Contract duration** | Decision **C3(b)** (ACK, 16 Sep): shorter-dated expressions are allowed; each carries `last_exit_session` = min(Day 20, expiry minus exit buffer); valuation exits unresolved paths at that session's bid. "Decided by valuation and ranking, not by a DTE rule" (spec, *Expression life*). | Hard ticket guard (d5ff55d): days to expiry ≥ 1.5 × hold and ≥ 21 days, else rejected. | Backtest: contracts with ≤ 10 days returned −75.6%; the expiry cap was the most common exit. **New finding:** `empirical_option_ev.py` never implemented C3 — it simulates the full window regardless of expiry and credits moves that happen after the contract has expired, so it overvalues short-dated contracts. | (a) Reaffirm C3(b): implement `last_exit_session` in valuation and retire the hard guard, keeping only the tradeability floor "expiry before the approved minimum usable session". (b) Supersede C3 with the hard guard. | **(a).** The defect is in valuation, not in the absence of a rule. Implementing C3 penalises short contracts honestly, keeps volume, and honours R11. Measured on the backtest ledger before the guard is retired. | **(a) — ACK, 18 Sep 2026.** C3(b) reaffirmed; hard duration guard retired; valuation decides. |
| **D2** | **Holding horizon** | Thesis window is 1–20 sessions; "no fixed holding bucket (no 5 / 10 / 20 choice)"; expected resolution comes from the first-passage distribution (spec, *Trade-window behaviour*). | Proposed (not built): the router's `planned_hold_sessions` (5/10/20) as the single hold. | The actuarial `recommended_hold_days` is a fallback default of 20 in 1,368 of 1,498 rows (`actuarial_query.py:1128`) — which happens to equal the specification's maximum window. The router's 5/10/20 bucket drives contract selection only. | (a) Follow the specification: value over the thesis window (20) capped by each contract's `last_exit_session`; router buckets descriptive only; the actuarial default made explicit (R1). (b) Adopt the router bucket as the hold. | **(a).** Withdraws my fix-2 proposal. The selector's session/calendar unit mismatch (R3) is still fixed. | **(a) — ACK, 18 Sep 2026.** Thesis window 1–20 sessions, capped per contract by its last exit session. |
| **D3** | **Issuing when value is negative** | RAEV ≤ 0 stays visible as `NO_POSITIVE_EDGE`, not a buy (spec, *Negative edge*). Ranking key is RAEV per $ at risk (decision C9). | Fix 1 (025c284): top 5 by cautious value are issued daily even if every value is negative (ACK requirement: no zero-trade days). | Top 5 per session: −18.8% mean against −49.7% for the old gate; ranking correlation 0.44. RAEV is not yet implemented; the cautious value is an interim key. | (a) Keep the daily top 5 as V1 decision support, each ticket labelled `NO_POSITIVE_EDGE` when its value ≤ 0. (b) Issue only positive values (zero-ticket days return). | **(a)**, recorded as a V1 presentation rule; switch the key to RAEV when it exists. | |
| **D4** | **Underlying stop** | Invalidation is the structural stop; paths end at its touch (spec §10–§12). | Proposed: remove the underlying stop inside the window. | Paired test on 638 trades: +2.9 points, t = 3.21, profitable share 30.7% → 36.5%. Stops sat at 0.6 expected moves; stop exits faced a 71% median exit spread (fill study rows). | (a) Remove the stop as an option exit trigger; invalidation is recorded as a thesis state but does not force an option exit before `last_exit_session`. (b) Keep. | **(a)**, as a new exit-policy version measured on the ledger. | |
| **D5** | **Execution cost** | Ask-in / bid-out until our own fills validate anything better (spec; method notes 03, 06). | Research uses a 30% effective-spread model (Muravyev-Pearson) as a sensitivity. | Fill study: quoted −49.1%, timed −32.2%, mid −18.1%. | (a) Keep ask/bid as the standard; report timed and mid as sensitivity only until own fills calibrate. (b) Adopt 30%. | **(a)** — agrees with the specification; no change. | |
| **D6** | **Events and options** | Event and macro information is display-only (CLAUDE.md rule 6; spec §14, §26). | Research proposes an earnings sleeve driving long options. | Published evidence (Chung-Louis) is for straddles, not single legs; untested here. | (a) Event sleeve is shadow research only; no authority, no ranking input. (b) Amend rule 6. | **(a)** — no change to authority. | |
| **D7** | **Stale quotes** | A stale quote is a tradeability exclusion (spec, *Tradeability*). | ACK (17 Sep): flag quote age, never gate on it; delay-adjusted freshness implemented (f627ecb). | The provider's 15-minute delay made every quote "stale" on raw age. | (a) "Stale" means older than the governed window **net of the disclosed provider delay** (as implemented); older quotes are excluded, prior-session quotes flagged. (b) Never exclude on age. | **(a)** — reconciles both. | |
| **D8** | **V1 expression scope** | Decision Q1: long calls/puts, debit verticals, long shares (bull), short shares (bear, borrow permitting). | V1 blueprint: long calls/puts and long shares only. | Share cost model not yet credible; borrow not modelled. | (a) V1 = long calls/puts + long shares; verticals and short shares **deferred, not removed**. (b) Full Q1 scope now. | **(a)** | |
| **D9** | **Composite signal as a direction source** | Direction has one owner (spec, C5 Thesis). | Drift calibration composite (5-day reversal, 52-week low, 12-1 momentum). | It **failed its pre-registration** at the primary 5-session horizon (t 1.26–1.68 against 3). | (a) Shadow research only; no direction authority. (b) Second direction source. | **(a)** | |

## Consequences once decided

- **D1(a)** reverts the hard guard in d5ff55d to the tradeability floor only, after the valuation change is measured.
- **D2(a)** withdraws the fix-2 proposal to make the router's bucket canonical; fix 2 becomes "implement C3 in
  valuation" plus the unit fix in the selector.
- Specification v1.1 is amended by reference to the decided rows; the pipeline map and knowledge README are marked
  where they are superseded (blueprint, *document hygiene*).
