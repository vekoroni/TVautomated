# Deployment receipt — 17 Sep 2026 (branch `avs-fix-001`)

**Operational decision (ACK, 17 Sep 2026):** too many gaps to use the pipeline for signals today; no morning run. Approved fixes are deployed to production and verified before the US close; the full evening run after the close is the first end-to-end use.

## What was deployed (all test-first; rules failed before and pass after)

| Merge | Fix | Tests |
|---|---|---|
| `202836b` | Layer 3 GARCH(1,1) fallback works (ndarray access, scale conversion, failures logged); fallback order HAR-RV → GARCH(1,1) → EWMA; EGARCH removed (model comparison: HAR-RV QLIKE 0.511, GARCH(1,1) 0.593, EGARCH 319) | `tests/test_layer3_garch_fallback.py` F1–F6 |
| `3fbd863` | **E1/E2** `select_best_contract` chooses only tradeable contracts: two-sided quote and measured spread (`(ask-bid)/mid`) within the horizon limit (1–5d 15%, 6–10d 25%) — the terminal gate's authority. **E6** `_governed_structural_target`: target positive and on the thesis side, else absent with `structural_target_state` | `tests/test_wp1_expression_tradeability_rules.py` T1–T6 (13) |
| `db2d807` | Layer 3 integrity: macro regime display-only (×1.10 / ×1.05 multipliers and RISK_OFF confidence penalty removed); missing price history → no forecast (`l3_forecast_state`), missing IV → no tailwind (`l3_iv_tailwind_state`), three hidden 0.25 defaults removed; `garch_runner` counts and logs per model (HAR-RV no longer reported as GARCH); consumers (`layer4_mispricing`, `empirical_option_ev`, superbrain expensive-vol gate, Intelligence Lab stats/priority) handle missing explicitly | `tests/test_layer3_volatility_integrity.py` (28) |
| `45bcd7d` | Book integrity: **E3** missing ask/mid/premium/spread stay missing (never 0.0); **E4** prices belong to the contract written (reselected contract gets no borrowed quote; 0.0 ask not a price); **E5** wrong-side contract removed from the option expression, share expression kept (`OPTION_EXPRESSION_REMOVED_CONTRACT_SIDE_CONFLICT`); **E7** invalidation side-checked, never a wrong-side legacy stop; one owner for the side rule (`contracts/thesis_geometry.invalidation_on_thesis_side`) | `tests/test_book_integrity_e3_e4_e5_e7.py` (21) |

## Verification (no provider calls)

- **Tests (main tree, merged code):** all new rule files pass; related existing files pass with unchanged counts (options: w16 spread authority 15, w33 taxonomy 24, EV3 handoff 38, liquidity lifecycle 16, crossed quotes 8, DTE policy 9, research contract 11, pre-evening repairs 10, direction geometry 9, macro enrichment 1; Lab: governed handoff 15, big bang 3, monetisation gate 6, morning handoff finalizer 9, EOD research handoff 5, WS2 trigger spine 6; volatility: GARCH regime 6, empirical option EV 20; MSI functionality 62 and regression 29; rebuild package 109). Orchestrator, runner, EOD, morning gate and options modules import cleanly.
- **Pre-existing failures (identical on the pre-change code; not caused by this deployment):** `tests/test_dynamic_session_phase2.py` 1, `tests/test_avs_fix_002_stages2_5.py` 2, `tests/test_options_liquidity_morning_lab.py` 1, `tests/msi/test_flow.py` 1, `tests/test_avs_fix_001_w35_opportunity_tier.py` 20.
- **Selector replay on recorded chains (run 20260916_223756, 1,339 tickers):** old-code replay reproduces 1,182 of 1,333 recorded selections (89%). New code: 559 tickers with a contract (327 unchanged, 232 switched to a tradeable alternative), 774 with no tradeable contract (option expression dropped, shares kept); median spread of chosen contracts 40.5% → 16.4% of mid; none above the limit; no one-sided quotes.
- **Book replay** (`build_final_opportunity_book` on recorded Lab inputs, old code `202836b` vs new): 16 Sep identical (1,476 rows, same verdicts and states — inputs already clean). 14 Aug: wrong-side contracts 79 → 0 (79 recorded as removed), verdicts unchanged. 24 Aug: 30 → 0 (30 removed), zero ask 1 → missing, verdicts unchanged.
- **Evening plan (`--evening --plan-only`, 10:28 UTC):** BUILD_THESIS for last completed session 2026-09-16, stages DISCOVERY → COMPLETED_MARKET_PROFILE → VANGUARD → OPTIONS → PUBLISH_THESIS; no side effects.

## Expected effects on tonight's evening run
- Far fewer option expressions (replay: ~42% of tickers keep a contract); remaining contracts are two-sided and within spread limits. Tickers without a tradeable contract carry the share expression.
- Layer 3 volatility values ~5–10% lower than last night where the macro regime was TRANSITIONAL/BEARISH (multiplier removed); some rows now show missing volatility / tailwind states instead of 25% / neutral.
- Lab priority: a missing IV tailwind now scores 0 (was neutral 2.5 points).

## Open decisions (ACK)
1. **Resolved (ACK, 17 Sep):** side conflict with an active gate `final_action` (e.g. BUY_NOW) is downgraded to CONTRACT_REPAIR; protective actions never upgraded (commit after 620a35a).
2. **Resolved (ACK, 17 Sep):** invalidation with no reference price is kept and flagged unverified; Lab missing-IV tailwind scores 0 (approved).
3. Remaining fields that still describe the pre-reselection contract (`morning_selected_contract_symbol`, morning/current contract quotes, EOD greeks/OI 0.0 defaults).
4. Layer 3 5% floor / 250% cap still unvalidated literals; forecast CSV omits tickers without price data.
5. Acceptance against reality: outcome scorer on forward sessions (DQ-1…DQ-3, spread cost, return on premium) — first evidence after tonight's run.

## Addendum — afternoon deployments (17 Sep 2026)

| Commit | Change | Tests / evidence |
|---|---|---|
| `e0fa763` | E5 follow-up (ACK): active gate action on a side-conflict row downgraded to CONTRACT_REPAIR; protective actions never upgraded | 6 downgrade cases failed before, pass after; Lab / handoff / OLM / MSI logic unchanged |
| `6d73429` | Item 1(a): C12 outcome scorer runs after every completed evening run (non-critical, observation only; `diagnostics/outcome_scoring_<run_id>.json`) | S1–S5; real run on 20260916_223756 COMPLETED in 47 s |
| `ff2c035` | Item 1(b) book leftovers: L1 all contract-describing fields (62 in the Lab book, plus morning-gate repair fields) describe the contract written or are missing with a reason — a removed wrong-side contract can no longer return through `morning_selected_contract_symbol`; L2 unobserved EOD greeks / OI missing (not 0.0); L3 reselection flag reflects the final state | 24 tests (11 failed before); 54 files before/after unchanged; book replay 16 Sep and 14 Aug: rows, contracts and verdicts identical |
| `6b1fa4b` | Item 2 increment 1 (shadow, no caller yet): `empirical_option_ev` scales the pooled actuarial distribution to the ticker's own volatility (log space) and values exits at the bid | R1–R5; on 2,473 realised option marks rank correlation 0.109 → 0.158, lowest quintile realised −66% (win 5%) vs highest −27% (win 28%); level still optimistic (exit path not modelled) |

Open (ACK): a reselected row whose liquidity state is now missing is routed to MANUAL_REVIEW by the lifecycle guard; a reselected quote counts as established only with bid > 0 and ask > 0.
