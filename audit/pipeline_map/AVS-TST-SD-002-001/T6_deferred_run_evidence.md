# T6 — Fixes whose run-level effect cannot be proven offline

No run has been produced since the fixes. Pipeline execution is out of scope for this test. Stated once, here, and not repeated as a status anywhere else in this assessment: **for the fixes below I proved the code and the fixture-level behaviour; what a live cycle must show is one field or artefact each.**

| Fix | Verified offline | Single artefact/field a future run must show to close it at run level |
|---|---|---|
| **F22** Vanguard fail-open removed (flag-on path) | Governed branch: one daily bar and a 12-bar frame both give `NOT_EVALUATED`, `ready_to_trade=False`, `poc/VAH/VAL = None`, confidence 0.0, for CALL/PUT/UNRESOLVED inputs (`T1_xml/T1_phase4.xml`) | `vanguard_signals_enriched_<run>.csv`: **zero** rows with `auction_state=ALIGNED` and a null or zero POC — the AR-003 baseline was 1,449 zero-level / 995 ALIGNED |
| **F23** Completed-profile stage after Discovery, before Vanguard, persisted and cache-reused | Second execution issues 0 physical requests; stage order fixed in `BUILD_THESIS_STAGES` (`T1_xml/T1_phase4.xml`) | the run's completed-profile stage summary showing `physical_provider_requests: 0` on a warm rerun with `input_count == completed + deferred` |
| **F25** `validate_thesis`: underlying-first, survivor-only refresh, stop on invalidation | Callback-order and zero-later-call assertions hold; an invalidated ticker triggers no option and no bar callback (`T1_xml/T1_phase5.xml`) | the run's `api_request_ledger` rows showing **zero** `EXACT_OPTION_QUOTE` requests for any ticker whose validation event is `THESIS_INVALIDATED` |
| **F27** Profile lifecycle states across premarket / RTH / after-hours; EOD profile immutable | Premarket makes 0 bar-resolver calls and publishes `PENDING_MARKET_OPEN`; RTH publishes `DEVELOPING_SESSION` only after the underlying survives; after-hours without provider finalisation stays `PARTIAL_SESSION` at plan level (`T1_xml/T1_phase5.xml`, `T1_xml/T1_phase8.xml`) | the completed profile's `evidence_id` and content hash **byte-identical** in the EOD artefact and in every later validation event of the same session |
| **F28** Morning Market Structure provider swapped to governed MarketData 5-minute | `_fetch_polygon_minute_bars` is gone; the call site passes `interval_minutes=5`, `provider="MARKETDATA"` | the run's `api_request_ledger` showing `provider=MARKETDATA` and **no** `api.polygon.io` request from the `MARKET_STRUCTURE` stage |
| **F29** `ms_*` evidence copied into Lab result rows, not only `live_map` | `row.update(live)` / `row.update(unavailable_fields)` at `morning_gate.py:3463-3528`; fixture row carries the fields (`test_morning_profile_evidence_is_written_to_result_row`) | `final_opportunity_book_<run>.csv`: `ms_profile_evidence_state` populated on every worklisted row (it is absent from every row of run `20260901_082437`) |
| **F31** Dynamic dispatcher (governed pointer, thesis reuse, plan-before-callback, new run id on finalisation) | Unit-level only, as instructed — no CLI was invoked; the CLI branch was traced statically (`intelligent_orchestrator.py:6342-6440`) | a persisted `run_plans.sqlite` row whose `plan_hash` matches the plan recorded in the run directory, written **before** the first provider request in `api_request_ledger` |
| **P0-02 flag-off** (not a fix — a gap) | The current production path is unchanged; `AuctionStateSynthesizer` takes the legacy branch and rebuilds an intraday profile from daily OHLCV | this cannot be closed by a run at all until `AVSHUNTER_COMPLETED_PROFILE_ENABLED` is enabled. See DEF-019 |
| **P0-07 data level** | Contract-level only: frozen geometry, symmetric CALL/PUT transitions, explicit defer on missing data | `final_run_manifest.json`: `system_defects.missing_selected_handoff.invalidation_spot == 0`. The latest run (`20260902_232526`) still reports **127** |
