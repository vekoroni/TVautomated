# Track H — Ledger, fills and outcomes (REQ-WP7-01…03)
No NORMAL_COMPLETED_SESSION run exists; every result below is on TEST-condition runs and cannot be CLOSED.
Runs used: primary 20260911_115904 (TEST, dirty tree, pre-remediation code 00baa2b), comparison 20260910_150045 (TEST), older full-artefact runs 20260904_004338 and 20260831_010309 (no ledger events exist for them), ledger runs 20260905_151448 / 20260906_213931 / 20260909_071646 / run_001.
Data sources: `db_copies/decision_outcome_ledger.sqlite` (8,290 `ledger_events` rows, opened `mode=ro`), `db_copies/trade_journal.db`, `db_copies/historical_prices.sqlite` (2,294,796 `ohlcv_daily` rows), `data/output/runs/*/diagnostics/outcome_maturation_<run>.json` (4 runs), `probes/p31_presented_candidates.csv` (Track census, 17,116 presented rows). Source: `domain/decision_outcome.py`, `canonical_data/decision_outcome_ledger.py`, `canonical_data/outcome_maturation.py`, `canonical_data/outcome_learning.py`, `canonical_data/historical_prices.py`, `intelligent_orchestrator.py:2159-2320`, `tools/record_manual_decision.py`. Probes: `probes/p14_EH_ledger_census.py` (→ `p14_EH_ledger_census.csv` long format run_id,event_type,direction,n; `_wide.csv`; `_out.json`; `p14_EH_ledger_payload_keys.json`), `p14_EH_h2_trade_journal.py` (→ `_out.json`), `p14_EH_h3_eligibility.py` (→ `_out.json`), `p14_EH_h3_price_store.py` (→ `_out.json`, `_per_ticker.csv`), pytest outputs `p14_EH_pytest_ddd_decision_outcome.txt`, `p14_EH_pytest_stages2_5_fill.txt`, `p14_EH_pytest_stage6.txt`.

## Results
| REQ ID | check | CALL result | PUT result | OTHER result | source evidence (file:line) | run evidence (run_id, artefact, field, value) | verdict | closed-offline? |
|---|---|---|---|---|---|---|---|---|
| REQ-WP7-01 | H1a ledger census run × event_type; decisions and fills separate event types | CANDIDATE_DECISION 3,023; VALIDATION 949; EXECUTION_DECISION 949; OUTCOME 1 | CANDIDATE_DECISION 1,720; VALIDATION 542; EXECUTION_DECISION 542; OUTCOME 0 | CANDIDATE_DECISION 188; VALIDATION 188; EXECUTION_DECISION 188 (all 20260911, STRANGLE/UNRESOLVED) | `domain/decision_outcome.py:23-35` vocabulary (10 types incl. `PRESENTATION_DECISION`, `FILL_RECORDED`); `canonical_data/decision_outcome_ledger.py:208-220` `event_counts` groups by event_type | ledger copy: event types present = {CANDIDATE_DECISION 4,931, VALIDATION 1,679, EXECUTION_DECISION 1,679, OUTCOME 1}; `PRESENTATION_DECISION` 0, `FILL_RECORDED` 0, `OUTCOME_OBSERVATION` 0, `THESIS_CREATED` 0, `TRADE_ENTRY` 0, `DATA_EXCEPTION` 0 (`p14_EH_ledger_census_out.json`) | OPEN at run level (fill/decision record types exist in code but no stored run or ledger row exercises them) | CLOSED OFFLINE for schema + separation (`tests/test_ddd_decision_outcome.py` 18 passed; `tests/test_avs_fix_002_stages2_5.py::test_decision_and_fill_are_separate_append_only_events` 1 passed) |
| REQ-WP7-01 | H1b `decision_record_v2` / `fill_record_v1` schemas + manual entry CLI/form | n/a (no rows) | n/a | n/a | `decision_outcome_ledger.py:247-264` (`decision_record_v2`: preferred_assessment_id, presentation_summary, human_response ∈ {TAKEN, NOT_TAKEN, DEFERRED}, response_timestamp_utc → event type `PRESENTATION_DECISION`); `:267-286` (`fill_record_v1`: occ_symbol, price>0, quantity>0, side ∈ {BUY, SELL}, fill_timestamp_utc, source ∈ {MANUAL_CONFIRMATION, BROKER_IMPORT} → `FILL_RECORDED`); CLI `tools/record_manual_decision.py:8-27` (side hard-coded `BUY`, source hard-coded `MANUAL_CONFIRMATION`); no Lab form found (grep `fill_record` in lab modules: 0 hits) | none — 0 `PRESENTATION_DECISION`, 0 `FILL_RECORDED` in ledger copy | NOT TESTED at run level (nothing entered; rule H2 "enter nothing") | CLOSED OFFLINE (schema functions + CLI exist; unit test passes) |
| REQ-WP7-01 | H1c append-only enforcement | n/a | n/a | n/a | `decision_outcome_ledger.py:87-92` SQL triggers `ledger_events_no_update` / `ledger_events_no_delete` RAISE(ABORT); `:114-115` `append()` raises when `read_only`; `:119-128` identical event_id with different payload → RuntimeError; `:129-139` previous_event_id must exist + `validate_event_link` | ledger copy schema (`p02_db_schema.json`) has the two triggers; no evidence of update/delete attempts on stored runs | CLOSED OFFLINE | yes (`test_ddd_decision_outcome.py` 18 passed incl. read-only test in stage6 pack) |
| REQ-WP7-01 | H1d no position episode without a `FillRecorded` | 0 fills / 0 episodes | 0 / 0 | 0 / 0 | no "position episode" object exists in code (`grep -i "position\|episode"` over ledger payload_json: 0 hits; only OUTCOME payload hits `mfe`/`mae`/`horizon`) | ledger copy payload substring hits: `OUTCOME|mfe 1, OUTCOME|mae 1, OUTCOME|horizon 1`; `position`/`episode`/`fill` 0 | NOT IMPLEMENTED (position-episode concept absent; vacuously no episode without a fill) | — |
| REQ-WP7-01 / OPS-06 | H2 manual fills carry symbol, price, quantity, time, source | ledger fills 0; journal `closed_trades` CALL 5 | ledger fills 0; journal PUT 8 | journal OTHER 1 (trade_id 2, empty direction) | `avshunter_trade_journal.py` schema (`p02_db_schema.json` `closed_trades` 129 cols): no `occ_symbol`/`contract_symbol` column, no `fill_source` column, no `entry_time` column | `trade_journal.db` copy: `closed_trades` 14 rows, `trades` 0 rows; `journal_entry_source` = `BROKER_CONFIRMATION_IMPORT` 8 rows, NULL 6 rows; every row missing OCC symbol (14/14), entry time-of-day (14/14), fill source per `fill_record_v1` vocabulary (14/14); ticker, entry_premium, contracts, entry_date present 14/14; strike/expiry missing on 1 (trade_id 2) | NOT TESTED (no `FillRecorded` exists to verify); journal rows are NOT fills under REQ-WP7-01 (no `MANUAL_CONFIRMATION`/`BROKER_IMPORT` source, no OCC symbol) | — |
| REQ-WP7-02 | H3a price-store health-check at run start and absent-store behaviour | see H3c | see H3c | — | `intelligent_orchestrator.py:2218-2237` absent file → `status=DEFERRED`, `reason=CANONICAL_PRICE_HISTORY_UNAVAILABLE`, `logger.warning`, artefact written; `:2245-2262` `HistoricalPriceDatabase.health()` (`canonical_data/historical_prices.py:498-525`: schema validity, `PRAGMA integrity_check`, rows, tickers, first/last date, partial rows) → `DEFERRED` / `CANONICAL_PRICE_HISTORY_HEALTH_FAILED`; `:2311-2318` any exception → `status=FAILED`, warning, never aborts. The health check runs inside the maturation stage (`:6633`, after the run), not "at run start" | all 4 stored diagnostics show `status=COMPLETE`, `price_database_path=data/canonical/historical_prices.sqlite`; none carries `price_database_health` (pre-remediation shape) | OPEN (health-check exists but is post-run; absent-store path writes no `outcomes_deferred` count — see UAT-D-H2) | CLOSED OFFLINE for absent-store deferral (`test_avs_fix_002_stage6_outcome_learning.py` 22 passed) |
| REQ-WP7-02 | H3b why 3,252 candidates were ineligible; visible or silent | 2,074 ineligible (1,758 session-only; 39 +target; 277 +target+invalidation) | 1,178 ineligible (967; 100; 111) | 0 EOD_THESIS OTHER rows (188 OTHER are `MORNING_VALIDATION` stage, filtered out at `outcome_maturation.py:77-81`) | eligibility rule `canonical_data/outcome_maturation.py:107-131` (quoted in notes); `completed_session` sourced at `decision_outcome_ledger.py:333-337`; `planned_hold_sessions` at `:351` | ledger copy: 3,252/3,252 EOD_THESIS candidates have `completed_session=None` → `COMPLETED_SESSION_UNAVAILABLE`; 527 also `target_price` empty/≤0; 388 also `invalidation_price` empty; `planned_hold_sessions=None` 3,252/3,252 (`p14_EH_h3_eligibility_out.json`). Diagnostics 20260911: `candidate_events 3252, eligible 0, ineligible 3252, deferred 0`; 20260910: 2034/0/2034/0; 20260909: 793/0/793/0; 20260906: 558/0/558/0 — no reason field, no `OUTCOME_OBSERVATION` rows (0 in ledger) | OPEN — P1 (UAT-D-H1): the stored artefact reports a count but not the reason; the reason lives only in `OUTCOME_OBSERVATION` events that were never written | current code would write `DATA_EXCEPTION` observations with reason string (`outcome_maturation.py:125-131`) — CLOSED OFFLINE only |
| REQ-WP7-02 | H3c price-store coverage for governed universe | 798/798 primary CALL rows covered | 458/458 | 188/188 | `historical_prices.py:426-462` `read()` filters `bar_status='COMPLETE'`, `adjustment_convention='POLYGON_SPLIT_ADJUSTED'` | copy: 3,618 tickers, 2021-08-23 → 2026-09-10, 2,294,796 rows all `COMPLETE`/`POLYGON`; primary book 1,444/1,444 tickers present, rows per ticker min 214 / median 1,268 / max 1,268, all with last bar 2026-09-10 (store lagged one session at copy time 12 Sep; 11 Sep close absent) (`p14_EH_h3_price_store_out.json`) | store is present and healthy for the universe; deferral reason on stored runs is NOT "store absent" and NOT "horizon not elapsed" — it is missing `completed_session` (H3b) | — |
| REQ-WP7-02 | H4 counterfactual `OutcomeRecord` per presented candidate (MFE/MAE, attribution, horizon) on runs ≥ 5 sessions old | 20260904: 164 presented, 0 outcomes; 20260831: 77 presented, 0 outcomes | 20260904: 92, 0; 20260831: 104, 0 | 20260831: 20, 0 | `outcome_event_from_candidate` `decision_outcome_ledger.py:511-544` payload = `OutcomePathEvaluation` fields (`mfe`, `mae`, `horizon_sessions`, `first_passage_state`, `target_first_hit_session`, `stop_first_hit_session`, `directional_return`) + `is_counterfactual=True`, `counterfactual_scope=ALL_PRESENTED`; attribution NOT in the event — it is computed only in the learning record (`domain/outcome_learning.py:131-154`, field `attribution` `:184`) | coverage 0/256 (20260904) and 0/201 (20260831): those runs have no ledger events at all (ledger starts 2026-09-05); across the whole ledger 1 OUTCOME (run_001 `QA_CLOSE`, `is_counterfactual=False`, `mfe`/`mae` present as keys but for a test row) vs 3,252 EOD candidates → 0/3,252 | NOT IMPLEMENTED at run level | CLOSED OFFLINE (`test_all_presented_candidates_mature_at_six_horizons`, `test_incomplete_and_bad_candidates_receive_named_observations` pass) |
| REQ-WP7-03 | family-member outperformance field | 0 records | 0 records | — | `domain/outcome_learning.py:183` `family_member_outperformance_state`; `canonical_data/outcome_learning.py:220-237` computed only from `doi_outcome_labels` join (`load_option_outcome_lookup` `:171-198`), else `NOT_EVALUATED`; test `tests/test_avs_fix_002_stage6_outcome_learning.py:361` | `control_plane.sqlite` copy `doi_outcome_labels` 0 rows, `doi_contract_assessments` 0 rows; readiness snapshot `records: []` → field never populated on any run | NOT IMPLEMENTED at run level (field exists offline; not on `OUTCOME` events, only on learning records) | CLOSED OFFLINE |

## Per-check notes
**H1.** `python probes/p14_EH_ledger_census.py` rebuilds `p14_EH_ledger_census.csv` (21 rows, long format). Totals: CANDIDATE_DECISION 4,931 (CALL 3,023 / PUT 1,720 / OTHER 188), VALIDATION 1,679 (949/542/188), EXECUTION_DECISION 1,679 (949/542/188), OUTCOME 1 (CALL). Per run: 20260905 294; 20260906 264; 20260909 470+235+235; 20260910 1,241; 20260911 2,662+1,444+1,444; run_001 1. `previous_event_id` is set on all 1,679 EXECUTION_DECISION (→ VALIDATION) and on none of the CANDIDATE_DECISION/VALIDATION/OUTCOME rows. Vocabulary (quote, `domain/decision_outcome.py:23-33`): `THESIS_CREATED, CANDIDATE_DECISION, VALIDATION, EXECUTION_DECISION, PRESENTATION_DECISION, FILL_RECORDED, TRADE_ENTRY, OUTCOME_OBSERVATION, OUTCOME, DATA_EXCEPTION`. The design vocabulary `HumanDecisionRecorded` / `FillRecorded` (`docs/AVS-SD-FIX-002…md:835-836`) maps to `PRESENTATION_DECISION` / `FILL_RECORDED` (deviation D-H1). `decision_record_v2` and `fill_record_v1` are payload `record_version` strings, not event types. `event_counts(run_id)` (`:208-220`) gives decision and fill counts as separate rows of one GROUP BY — "separate queries" in the sense of separate event_type keys.
**H2.** `python probes/p14_EH_h2_trade_journal.py`. 14 `closed_trades` rows (runs 20260305 → 20260624, all pre-remediation; 5 CALL, 8 PUT, 1 blank). Per record missing fields listed in `p14_EH_h2_trade_journal_out.json.per_record`: all 14 lack an OCC symbol column (only ticker/strike/expiry), all 14 lack time-of-day, 6 lack any `journal_entry_source`; 8 carry `journal_entry_source=BROKER_CONFIRMATION_IMPORT`, which is not in the `fill_record_v1` source vocabulary. None of these rows is a fill under REQ-WP7-01; nothing was entered by this tester. `calibration_reports` (10 rows, June 2026) is the legacy journal win-rate report, not ALG-09 (see Track E3).
**H3.** Eligibility rule (quote, `canonical_data/outcome_maturation.py:107-131`): candidates are `CANDIDATE_DECISION` with `decision_stage == "EOD_THESIS"` (`:77-81`); then `direction ∈ {CALL, PUT}`, `completed_session` non-empty, `reference_price`, `target_price`, `invalidation_price` each finite and `> 0` (`_number`, `:46-51`); any miss → `ineligible += 1` and six `DATA_EXCEPTION` `OUTCOME_OBSERVATION` events with reason `"|".join(missing)` (`:125-131`). Applying that rule to the ledger copy reproduces the primary diagnostic exactly (3,252 EOD candidates → 0 eligible) and gives the reason the artefact omits: `completed_session` is `None` on 3,252/3,252 (`decision_outcome_ledger.py:333-337` sources it from row `completed_session`/`evidence_session_date`/`bar_data_asof` or `run_metadata.completed_session`; the stored runs supplied none). `planned_hold_sessions` is `None` on 3,252/3,252 (`:351` reads only `row["planned_hold_sessions"]`; the books carry `hold_period` buckets), so even matured outcomes would be `PLANNED_HOLD_UNAVAILABLE` and never fit-eligible (`canonical_data/outcome_learning.py:246-247`). Visibility: stored diagnostics (pre-remediation shape) carry `ineligible_candidates=3252` with no reason and no `OUTCOME_OBSERVATION` rows — a count without a cause, i.e. silence about the mechanism; the remediated code names the reason only inside ledger observation events, and the run-level JSON still does not aggregate reasons. Current (cc509cb) writer: `intelligent_orchestrator.py:6552-6554` passes `run_metadata.completed_session = run_meta.dynamic_plan.last_completed_session` and `:3488-3490` derives `planned_hold_sessions` from `horizon_bucket` ({1_5d:5, 6_10d:10, 11_20d:20}) on the EV3-patched frame; whether that frame is the `_decision_rows` handed to `candidate_events_from_rows` (`:6564-6570`) is unverified, and no stored run was produced by this code (both TEST runs are 00baa2b). Absent store: `intelligent_orchestrator.py:2225-2237` writes `status=DEFERRED, reason=CANONICAL_PRICE_HISTORY_UNAVAILABLE` and a warning, but no `outcomes_deferred`/deferred-count key (REQ acceptance expects `outcomes_deferred > 0`). Health-check position: inside `run_outcome_maturation_stage`, called at `:6633` after the pipeline — not at run start. Price store: `python probes/p14_EH_h3_price_store.py`.
**H4.** Runs 20260904_004338 (d = 2026-09-03, 256 presented: CALL 164 / PUT 92) and 20260831_010309 (d = 2026-08-28, 201 presented: CALL 77 / PUT 104 / OTHER 20) have zero ledger rows of any type; `p31_ledger_events_by_run.csv`. OUTCOME events per run: run_001 1, all others 0. Coverage = 0 / 256 and 0 / 201. The one OUTCOME payload has keys `mfe, mae, outcome_horizon_sessions, days_held, exit_reason=QA_CLOSE, is_counterfactual=False` (`p14_EH_ledger_payload_keys.json`) — a trade-exit shape (`outcome_event_from_trade` `:459-508`), not a counterfactual. Counterfactual payload (code) carries `mfe`, `mae`, `horizon_sessions`, `first_passage_state` but no attribution category; attribution (`THESIS / CONTRACT_CHOICE / LIQUIDITY / EXECUTION / VOLATILITY / TIMING / UNCLASSIFIED_UNCERTAINTY`, `domain/outcome_learning.py:31-38`) is derived in the learning snapshot only, and `EXECUTION`/`VOLATILITY` are never assigned by `attribute_outcome` (`:131-154`).

## Defects (UAT-D-H<n>)
**UAT-D-H1** — REQ-WP7-02 (ALG-08). Severity P1. Exposure CALL 2,074 / PUT 1,178 / OTHER 0. Every EOD candidate written to the ledger by the stored runs lacks `completed_session` (3,252/3,252 `None`) and `planned_hold_sessions` (3,252/3,252 `None`), so outcome maturation marks 100% ineligible and no counterfactual outcome can ever mature from these rows; the run artefact reports the count without the cause. Evidence: `probes/p14_EH_h3_eligibility_out.json` (`ineligibility_reason_counts`, `value_classes.completed_session = {"None": 3252}`); `data/output/runs/20260911_115904/diagnostics/outcome_maturation_20260911_115904.json` (`eligible_candidates 0, ineligible_candidates 3252`, no reason key); source `canonical_data/outcome_maturation.py:117-118,125-131`; `canonical_data/decision_outcome_ledger.py:333-337,351`. Reproduce: `python probes/p14_EH_h3_eligibility.py`. A fix closes when a stored run's `CANDIDATE_DECISION` payloads carry non-null `completed_session` (ISO date) and integer `planned_hold_sessions` for every directed row, `eligible_candidates > 0`, and the diagnostic (or ledger `OUTCOME_OBSERVATION` rows) names the reason for every ineligible row. TRACE: REQ-WP7-02 | ALG-08 | WP-7B | STAGE-6 | TRACK-H | EVIDENCE-outcome_maturation_20260911_115904.json | N=3252
**UAT-D-H2** — REQ-WP7-02. Severity P2. Exposure CALL/PUT/OTHER all (run-level). The absent-price-store path (`intelligent_orchestrator.py:2225-2237`) and the health-failed path (`:2252-2262`) publish `status=DEFERRED` with a reason string and a `logger.warning`, but no `outcomes_deferred` count and no per-candidate deferral, and the health check runs after the pipeline (`:6633`) rather than at run start as REQ-WP7-02 states. Evidence: source lines cited; stored diagnostics carry no `price_database_health` block. Reproduce: read the cited lines; no stored run exercised the absent path. A fix closes when a run with the store removed writes `outcomes_deferred = <presented candidates × horizons>` plus an alert artefact, and `run_meta`/pipeline integrity records the health check before Discovery. TRACE: REQ-WP7-02 | ALG-08 | WP-7B | STAGE-6 | TRACK-H | EVIDENCE-intelligent_orchestrator.py:2225-2262 | N=0
**UAT-D-H3** — REQ-WP7-02 / REQ-WP7-03. Severity P2. Exposure CALL/PUT. The counterfactual `OUTCOME` event (`decision_outcome_ledger.py:521-535`) carries MFE/MAE/horizon/first-passage but no attribution category and no family-outperformance field; both live only in the derived learning snapshot, which is not a ledger record and is empty on every run (`records: []`). REQ-WP7-02 says maturation "attributes the outcome category" and REQ-WP7-03 says "each outcome shall record" the family field. Evidence: `p14_EH_ledger_payload_keys.json` (OUTCOME keys), `audit/avs_fix_002/stage6/CURRENT_LEARNING_READINESS.json` `records: []`. A fix closes when a matured `OUTCOME` ledger event on a stored run carries `attribution` and `family_member_outperformance_state`. TRACE: REQ-WP7-03 | ALG-08 | WP-7B | STAGE-6 | TRACK-H | EVIDENCE-p14_EH_ledger_payload_keys.json | N=1

## Deviations
| claimed | found | evidence | severity |
|---|---|---|---|
| Design event names `HumanDecisionRecorded`, `FillRecorded` (`docs/AVS-SD-FIX-002…md:835-836`; REQ-WP7-01 acceptance "appears as `FillRecorded`") | Code vocabulary is `PRESENTATION_DECISION` and `FILL_RECORDED`; `decision_record_v2`/`fill_record_v1` are payload `record_version` values | `domain/decision_outcome.py:28-29`; `decision_outcome_ledger.py:258,280` | P3 |
| REQ-WP7-01: "A CLI or Lab form for manual fill entry ships" with source ∈ {MANUAL_CONFIRMATION, BROKER_IMPORT}, side BUY/SELL | CLI exists but hard-codes `side="BUY"` and `source="MANUAL_CONFIRMATION"`; SELL fills and broker imports cannot be entered through it; no Lab form | `tools/record_manual_decision.py:24-26` | P2 |
| Stage 6 claim sheet: "Capture every presented candidate … All-presented population and six-horizon reconciliation tests"; "Stored-run replays 20260910_150045 and 20260911_115904" | On both stored runs 0 of 3,252 candidates are eligible; `population_reconciled` false; 19,512 candidate-horizons unaccounted; the reconciliation exists only in unit tests | `CURRENT_LEARNING_READINESS.json` summary; `p14_EH_e4_snapshot_from_copies.json` | P1 (same root cause as UAT-D-H1) |
| Expectation H3: "deferred reason is horizon-not-elapsed rather than absent store" | Deferred count is 0 on every stored run; ineligibility is `COMPLETED_SESSION_UNAVAILABLE` on 100% of rows — neither horizon nor store | `p14_EH_h3_eligibility_out.json` | P1 (UAT-D-H1) |
| Briefing/prompt: ledger `OUTCOME` count "< 100", position episodes absent | Confirmed: 1 OUTCOME; and the OUTCOME is a non-counterfactual QA row, so counterfactual OUTCOME = 0 | `p14_EH_ledger_census_out.json` | — |
| Price store "present now (1 GB)" and current | Present (1,013,129,216 bytes) but last bar 2026-09-10 at copy time 2026-09-12 (11 Sep close not ingested) | `p14_EH_h3_price_store_out.json` `global_max_date` | P3 (freshness lineage) |

## Premise notes
| spec | measured | gap size |
|---|---|---|
| REQ-WP7-02 measures option MFE/MAE "where canonical observations exist" and joins DOI labels by `preferred_assessment_id` | `doi_contract_assessments` 0 rows and `doi_outcome_labels` 0 rows in the control-plane copy; `preferred_assessment_id` absent from all 4,931 candidate payload key sets → every outcome would be `UNDERLYING_ONLY` and attribution `UNCLASSIFIED_UNCERTAINTY` for TARGET_FIRST | 100% of population |
| `attribute_outcome` vocabulary lists EXECUTION and VOLATILITY | no code path assigns either (`domain/outcome_learning.py:140-154`) | 2 of 7 categories unreachable |

## Not tested / blocked
- H2 manual fill verification: NOT TESTED — no `FILL_RECORDED` event exists and the tester enters nothing (rule).
- H3 live `HistoricalPriceDatabase.health()` (`PRAGMA integrity_check` on the 1 GB copy): NOT TESTED — `connection()` opens read-write and commits; replicated the row/ticker/date queries read-only instead.
- H1d position episodes: NOT IMPLEMENTED — no episode object to test.
- H4 on 20260904 / 20260831: no ledger rows exist for those runs (ledger starts 2026-09-05); run-artefact candidates only.

## Expectation vs actual
| row | expectation (E column) | actual | gap |
|---|---|---|---|
| H1 ledger | CANDIDATE_DECISION > 1,000; EXECUTION_DECISION > 100; HumanDecisionRecorded 0; FillRecorded 0; OUTCOME < 100; no position episodes | 4,931; 1,679; 0 (type named `PRESENTATION_DECISION`); 0 (`FILL_RECORDED`); 1; none | none, naming deviation only |
| H2 manual fills | none → NOT TESTED | NOT TESTED; journal `closed_trades` 14 rows are not fills (no OCC symbol / fill source) | none |
| H3 price-store health | diagnostic present; `outcomes_deferred` present; store present; deferral = horizon not elapsed | diagnostic present with `deferred_horizons=0`; store present and covers 1,444/1,444; 100% ineligible for missing `completed_session`, not horizon | material — root cause differs (UAT-D-H1) |
| H4 counterfactuals | NOT IMPLEMENTED at run level | NOT IMPLEMENTED at run level (0 counterfactual OUTCOME; 0 events for older runs) | none |

## State log lines
step,state,n,duration,note
H1,DONE,4931,~10s,ledger census long CSV p14_EH_ledger_census.csv; 4 event types present; FILL_RECORDED 0; PRESENTATION_DECISION 0; tests 18+1 passed
H2,NOT_TESTED,0,~5s,no FILL_RECORDED; trade_journal closed_trades 14 rows lack OCC symbol/fill source/time-of-day
H3,DONE,3252,~60s,100% ineligible for COMPLETED_SESSION_UNAVAILABLE; planned_hold_sessions None 3252/3252; price store 3618 tickers to 2026-09-10 covers 1444/1444
H4,NOT_IMPLEMENTED,0,~5s,0 counterfactual OUTCOME events; 20260904 0/256; 20260831 0/201; attribution not on OUTCOME event
WP7-03,NOT_IMPLEMENTED,0,~2s,family_member_outperformance_state exists on learning record only; doi_outcome_labels 0 rows
