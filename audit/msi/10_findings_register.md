# MSI v1.1 — Findings Register

**Date:** 2026-08-30 | Design: AVS-SD-MSI-001 v1.1 | One row per test ID. Compiled from the five independent test-agent reports (`tests/msi/test_functionality.py`, `test_logic.py`, `test_computation.py`, `test_flow.py`, `test_regression.py`) plus `07_run_artefacts.md`. Every VERIFIED/PASS below was closed at both levels (source evidence + executed test); every UNVALIDATED/PARTIAL/FAIL states explicitly which level is missing or contradicted.

**Test runtime used throughout:** `.codex_python313_runtime/python.exe` with `PYTHONPATH=venv/Lib/site-packages` (per preflight; not `venv/Scripts/python.exe`).

## Functionality (F-01–F-15)

| ID | Status | Source evidence | Execution evidence | Observed vs. specified (failures only) |
|---|---|---|---|---|
| F-01 | VERIFIED | `canonical_data/marketdata_response.py:24,93` | `test_functionality.py` — parallel-array parse/reject, executed | — |
| F-02 | VERIFIED | `marketdata_response.py:148` | executed, `bid_size=12,ask_size=9` | — |
| F-03 | PARTIAL / BLOCKED (error category) | 5 fixtures content-mapped to ok/no_data/scalar/parallel-array/missing-field | executed for 5/6 categories; `error` marked `pytest.skip` | §16.1 requires a fixture for provider-**error** responses; no file among the 5 in `tests/fixtures/marketdata/` represents one. Finding against MSI-0. |
| F-04 | PARTIAL (FAIL on label) | `marketdata_response.py:51`, `market_observation_resolver.py:75`, `avshunter_options_intelligence.py:1492,1501,2530,2531` | executed; `'OBSERVED_POSITIVE' != 'OBSERVED'` | §24 Size-quality enum specifies `OBSERVED`; code emits `OBSERVED_POSITIVE` at 4+ independent call sites, consistently. MISSING/OBSERVED_ZERO/rejection-of-negative all PASS. |
| F-05 | PARTIAL (FAIL on label) | `marketdata_response.py:_quote` L79 | executed; `'INVALID' != 'CROSSED'` | §24/§18 specify `CROSSED` for bid>ask; code emits `INVALID` — deliberate, codified in the implementer's own suite (`test_msi_agent1_data_foundation.py:52`). Zero-bid/ONE_SIDED path is correct and does not itself contradict §18 (see `20_design_inconsistencies.md` item for the resolved ambiguity). |
| F-06 | VERIFIED | `canonical_data/option_identity.py:40` | executed, all 10 vectors + prefixed/padded variants, 30 sub-assertions | — |
| F-07 | FAILED | `contracts/selected_contract_economics.py:24-28,46-48,73-86` (own regex/normaliser, not an alias) vs. `canonical_data/contract_reference.py:16` (correct alias) | executed; behavioral divergence demonstrated (shape validation differs) | §22/§23 require exactly one active OCC normaliser; two independently-implemented ones exist and disagree on invalid input. |
| F-08 | FAILED | `market_observation_resolver.py:78` (computes correctly under generic keys); `morning_gate.py:918-931` (discards bid/ask/sizes, keeps only mid+depth_level) | executed, reproduced the discard with a real (non-live) resolver call | §3.1/§8.3/§11 require `underlying_nbbo_bid_size`/`underlying_nbbo_ask_size` to reach the Lab; the values are computed and then silently dropped before any canonical field name is populated. |
| F-09 | VERIFIED | `canonical_data/session_clock.py` | executed — Labor Day holiday, early-close, DST transition, weekend/holiday last-completed-session, 7 tests | — |
| F-10 | UNVALIDATED (function VERIFIED, integration not) | `session_clock.py::evaluate_freshness` | executed at function level; in-process scan found **zero production callers** of `evaluate_freshness()` | §7.4 requires session-aware freshness to gate observations; the shared function is correct in isolation but nothing in `morning_gate.py`/`evidence_resolver.py`/the materialiser calls it — `freshness_map` is instead built from ad hoc row fields including a hardcoded `"UNASSESSED"` literal. |
| F-11 | PARTIAL (FAIL on cross-run idempotency) | `market_observation_resolver.py:126-127,199-204` | executed; identical content under two `run_id`s produced two different `dataset_id`s | §23 explicitly excludes `run_id` from the content hash; the resolver embeds `run_id` into the payload before hashing, breaking idempotent registration across runs. Same-run idempotency and changed-content-creates-new-revision both PASS. |
| F-12 | VERIFIED | `canonical_data/gateway.py:33-51`, `request_ledger.py:94` | executed — blocked, ledger row recorded, zero fetch calls | — |
| F-13 | FAILED (systematic) | multiple files (see below) | executed enum audit across all 10 §24 enums | Run kind/status/handoff/assessment/freshness enums PASS. Size quality, quote quality, structure lifecycle, structure relationship and comparison enums have **no owning class**; values are scattered string literals, and undeclared values leak into persisted output (`MS_NONE` from `market_structure/lifecycle.py:11`, flowing into `ms_lifecycle` for most ordinary sessions; `INCOMPLETE` as an undeclared 6th quote-quality value). |
| F-14 | VERIFIED | `market_structure/service.py:61-73` | executed | — |
| F-15 | PARTIAL (FAIL on rounding) | `marketdata_response.py:_quote` (no rounding) vs. `market_observation_resolver.py:94` (explicit `round(...,4)`) | executed; bid=1.00001/ask=1.00003 → mid=1.00002 (5dp) | §8.2 requires 4-decimal-place persisted prices; honoured on the underlying-NBBO path, not on the option-chain/exact-quote path. Integer sizes, ISO-8601 timestamps, 0–1 ratio bounds all PASS. |

## Logic (L-01–L-12)

| ID | Status | Source evidence | Execution evidence | Observed vs. specified (failures only) |
|---|---|---|---|---|
| L-01 | FAILED / NOT_IMPLEMENTED | `contracts/interpreter_handoff_materializer.py:174-228` (`_bundle()` passthrough only) | executed via `materialize_interpreter_handoff` | §8.8 requires computed `comparison_status`; no function computes it anywhere — an incomplete row produces `quote_change_evidence: {}`, not even `BASELINE_MISSING`. |
| L-02 | FAILED / NOT_IMPLEMENTED | same as L-01 | executed | §8.8's `BASELINE_ZERO` never fires — same absence. |
| L-03 | PARTIAL | `market_structure/lifecycle.py:12` | executed, 10/11 table rows correct | A row with `prior=None` and acceptance already holding on first contact jumps straight to `MS_ACCEPTED`, skipping the design's sole `none→MS_DEVELOPING` transition. |
| L-04 | FAILED (partial pass) | `morning_gate.py:3261` — single call site | executed (call-count assertion) | `MS_FAILED` terminality PASS. "Evaluated on completed five-minute intervals" FAILS — the service is invoked once per Morning Gate run over the whole session-to-date range; no incremental 5-minute evaluation loop exists anywhere. |
| L-05 | VERIFIED (SEED_VALUE) | `market_structure/params.py` (`MS_PARAMS_V1`) | executed — 6 isolated-rule-failure fixtures, each correctly rejected | — (thresholds are the design's proposed seed, not yet MSI-4-calibrated; behavior correctly reads them from config) |
| L-06 | VERIFIED | `market_structure/lifecycle.py::direction_relationship` | executed, all 7 rows | NEEDS_DESIGN_CLARIFICATION: design doesn't state whether `COARSE_DATA_LOW_CONFIDENCE` counts as "insufficient quality" for the `INSUFFICIENT_DATA` relationship row — code treats it as such; not contradicted, just unstated. |
| L-07 | PARTIAL | `market_structure/profile.py` (grepped, zero `TRADE_LEVEL_CONFIRMED` emissions repo-wide) | executed | `ONE_MINUTE_ESTIMATED`/`INSUFFICIENT_DATA`(missing bars)/corporate-action→COARSE all PASS. `TRADE_LEVEL_CONFIRMED` is never emitted by any code path. "Coarse bars" has no independent detection — only the corporate-action override reaches COARSE; ordinary coarse (e.g. 15-minute) bars are silently treated as `ONE_MINUTE_ESTIMATED`. |
| L-08 | PARTIAL | `contracts/interpreter_handoff.py`, `pipeline_interpreter/evidence_resolver.py` | executed | ACCEPTED/PRODUCTION gate, Morning-Gate-COMPLETED gate, TRAJECTORY bypass, file-presence-≠-acceptance all PASS. The design's RESEARCH-run-kind carve-out (§13) is structurally unreachable — `validate_handoff_manifest`/`publish_handoff_manifest` hard-require `RunKind.PRODUCTION`. |
| L-09 | FAILED / NOT_IMPLEMENTED | same absence as L-01 | executed (absence confirmed) | §13 trajectory normalisation for changed contracts has no implementation anywhere. |
| L-10 | PARTIAL | `pipeline_interpreter/assessment_contract.py:81-96` (`_narrative_conflicts`) | executed | Governed-field code-fill, structured-field-override rejection, direction/contract narrative-override rejection, temperature=0, rejected-record preservation all PASS. `_narrative_conflicts` has no lifecycle-assertion pattern — a narrative explicitly contradicting the bundle's lifecycle publishes as VALID. |
| L-11 | VERIFIED | `pipeline_interpreter/macro_context.py` | executed | — |
| L-12 | FAILED (functional intent partially met under different names) | repo-wide grep, zero matches for all 4 literal codes | executed | None of `MORNING_EVIDENCE_MISSING`, `CONTRACT_REQUOTE_REQUIRED`, `BUDGET_EXHAUSTED`, `REFRESH_CONFLICT` exist anywhere active; actual codes differ (`NO_ACCEPTED_INTERPRETER_HANDOFF`, `BOOK_BUNDLE_IDENTITY_MISMATCH`, etc.). §16.2 budget/lock-timeout have no implementation at all. |

## Computation (C-01–C-12)

| ID | Status | Source evidence | Execution evidence | Observed vs. specified (failures only) |
|---|---|---|---|---|
| C-01 | PARTIAL | `marketdata_response.py:_quote` L72-90 | executed, hand-arithmetic match on mid/spread_pct | mid/spread_pct/ONE_SIDED all PASS. `spread_value = ask − bid` is never computed/persisted anywhere (zero grep matches). "One shared helper used by...quote-change code" is unverifiable since no quote-change code exists (see C-02/L-01). |
| C-02 | FAILED / NOT_IMPLEMENTED | `contracts/interpreter_handoff_materializer.py:174-228`; `contracts/lab_evidence_overlay.py` | executed absence confirmation | Corroborates L-01/L-02/W-04/W-07: §8.8 comparison arithmetic is entirely unimplemented — field names declared, never computed. |
| C-03 | VERIFIED | `canonical_data/intraday_bars.py::normalise_minute_bars` L35-61 | executed, hand-computed VWAP (100.0, 100.6) matched exactly; `vwap_provider` preserved separately | — |
| C-04 | VERIFIED | `market_structure/profile.py::round_to_tick`,`profile_bin_width` L25-32 | executed, hand-computed ATR-driven (0.05) and tick-driven (5.0) cases matched | — |
| C-05 | VERIFIED | `market_structure/profile.py::build_market_profile` L44-67 | executed, hand-derived tpo_count/POC matched | — |
| C-06 | VERIFIED | `market_structure/profile.py::_value_area` L35-41 | executed, hand-traced value-area bounds matched | — |
| C-07 | VERIFIED | same module | executed, total allocated volume = Σ bar volumes exactly | — |
| C-08 | VERIFIED | `market_structure/service.py::_metrics` L23-46 | executed, 3 hand-computed repair-pct cases (0.0/0.25/1.0) matched exactly, band transitions cross-checked against lifecycle | — |
| C-09 | VERIFIED | `market_structure/service.py::_metrics`, thresholds read from `MS_PARAMS_V1` | executed, pass case and single-threshold-fails case both matched hand arithmetic | — |
| C-10 | PARTIAL | `market_structure/service.py` (ms_evidence_id derivation) | executed | Determinism, version-sensitivity, order-sensitivity all PASS. But §23 literally specifies SHA-256; actual implementation is `uuid5` (SHA-1-based per RFC 4122), and lineage additionally folds in `run_id`/`input_hashes` not listed in §23's rule. |
| C-11 | VERIFIED | `market_structure/service.py::calculate_market_structure_evidence` | executed twice on identical fixture, zero differing fields excl. timestamp | — |
| C-12 | VERIFIED (SEED_VALUE) | `market_structure/params.py::MS_PARAMS_V1` | executed, all 6 values read directly and checked against §10.2 ranges | `calibration_status="PROPOSED_NOT_CALIBRATED"` — correctly one of the two design-permitted states, not a failure. |

## Flow (W-01–W-20)

| ID | Status | Source evidence | Execution evidence | Observed vs. specified (failures only) |
|---|---|---|---|---|
| W-01 | FAILED | `contracts/selected_contract_economics.py:212-385` (`_quote_record` drops sizes); `contracts/lab_control.py:63-395,2226` (`FINAL_BOOK_FIELDS` has zero `*bid_size*`/`*ask_size*` entries) | executed, full CDS→selected-contract→Lab-row chain walked | §3.2/§9.1 require sizes to survive to the selected contract and Lab row. CDS-layer persistence is correct; both downstream legs silently drop the fields. |
| W-02 | VERIFIED | `market_observation_resolver.py`, `request_ledger.py` | executed, 1 physical fetch across 3 resolves; §15 invariant holds | — |
| W-03 | PARTIAL | `morning_gate.py:783-931` | executed, cache-then-reuse and atomic publication correct | The Lab-row-construction step that would produce this data in practice is separately proven broken by W-01; `contract_bid_size` appeared in this test's bundle only because the test supplied it directly, bypassing that break. |
| W-04 | PARTIAL | `contracts/selected_contract_economics.py:254-386` (refetch/recompute correct); grep confirms no §8.8 computation exists | executed | Every-new-leg-fetched and contract-dependent-recompute both PASS. Cross-contract price-change suppression (`CONTRACT_CHANGED`, null fields) is NOT_IMPLEMENTED — same gap as L-01/C-02. |
| W-05 | FAILED / NOT_IMPLEMENTED | `pipeline_interpreter/evidence_resolver.py:149-217` | executed; `refresh_required` object produced but consumed nowhere in production code (grep: only the producer and the test itself) | §9.3 steps 5–8 (Interpreter-triggered CDS refresh, new bundle, overlay write) has no execution path. The fail-closed staleness guard itself (step 3–4 equivalent) is correct and a genuine PASS. |
| W-06 | VERIFIED | — | executed, network primitive patched to raise, never touched | — |
| W-07 | PARTIAL | `contracts/interpreter_handoff_materializer.py:174-228`, `contracts/lab_evidence_overlay.py` | executed on a synthetic input | Bundle and overlay values match for shared fields, but only because both paths mechanically copy the same named keys supplied by the test — no code computes a live refresh (see W-05), so this proves contract symmetry, not a functioning feature. |
| W-08 | VERIFIED | `contracts/interpreter_handoff.py:315-386` | executed — duplicate ticker, hash tamper, empty bundle, wrong run-kind/status all correctly rejected with the matching §24 handoff status; atomic rename confirmed (no orphaned temp sibling) | — |
| W-09 | VERIFIED | — | executed — all 12 §8.7 identity fields present; missing `thesis_id` blocks publish; unknown ticker fails closed | — |
| W-10 | VERIFIED | `pipeline_interpreter/evidence_resolver.py:89-99` | executed — newer-mtime decoy CSV ignored; manifest selection sorts by self-validated `published_at_utc`, never filesystem mtime; missing artefact → non-zero/error | — |
| W-11 | VERIFIED (mostly) | `pipeline_interpreter_commands.py:475-476,541-542` | executed for run_id-from-manifest and independent-artefact reconciliation | NEEDS_DESIGN_CLARIFICATION: design's narrower claim about legacy `pipeline_interpreter/lab_reconciliation.py` self-compare risk — that module is not in the §22 MSI scope and carries its own documented (unresolved) risk comment at line 142-143; not fixed by MSI, not claimed to be. |
| W-12 | VERIFIED | `contracts/lab_evidence_overlay.py:48-103` | executed — 7 protected fields all rejected (`OVERLAY_AUTHORITY_FIELD`), unknown field rejected, allowed set succeeds | — |
| W-13 | VERIFIED | `tools/msi_reconcile.py`; `morning_handoff_finalizer.py:305-345` | executed — CLI exits 1 on mismatch; finalizer raises `MorningHandoffError` and blocks `run_meta.json` acceptance | — |
| W-14 | VERIFIED | AST-scanned every active `.py` under the relevant directories | executed with network primitive patched to raise; never touched | — |
| W-15 | VERIFIED (one sub-clause code-read only) | source-scanned 3 files, zero references to forbidden functions | executed for the scanned parts; chart-presence-vs-prompt-contract clause is source-citation only (would require an LLM call, out of scope) | — |
| W-16 | PARTIAL | `pipeline_interpreter_commands.py:1,2141-2142,2168-2169,1397-1420,2047-2054` | executed — `/morning`/`/live` neutered, print retirement notices; `/inputs`/`/status` report manifest integrity; zero `"Archive"` references; `morning.bat` removed | Substance PASS. `/morning` and `/live` remain routable (neutered) command names rather than literally absent from the command table — a softer reading than "absent from production." |
| W-17 | NOT_ACTIVATED | `config/msi_runtime.json` — `screen_adapter: false` | executed flag check only, per instructions | — |
| W-18 | VERIFIED | `pipeline_interpreter/macro_context.py:98-151` | executed — forbidden authority key stripped and flagged; out-of-run-root reference rejected; zero glob/mtime/MA_Inputs references | — |
| W-19 | FAILED / NOT_IMPLEMENTED | `canonical_data/registry.py:224-244`; no lock/coalescing/budget code anywhere in `canonical_data/` | executed — two concurrent requests both physically fetched (no coalescing); losing thread can raise an unhandled `DatasetValidationError` instead of resolving idempotently | §16.2 request coalescing, lock timeout and budget ceiling have no implementation and no configuration values. Bonus finding: concurrent identical registration is not idempotency-safe, a robustness gap beyond the design's own stated concern. |
| W-20 | VERIFIED | `canonical_data/lifecycle.py` | executed — `FetchNotAuthorised`, zero fetch calls, ledger entry recorded; Interpreter-side also confirmed | — |

## Regression (R-01–R-09)

| ID | Status | Source evidence | Execution evidence | Observed vs. specified (failures only) |
|---|---|---|---|---|
| R-01 | VERIFIED | `market_structure/lifecycle.py:19-25`; `morning_gate.py:2957-2988,3170-3280`; `execution_gate.py` (zero `market_structure` references) | executed, all 15 table rows/branches + JSON-diff byte-equivalence for all 4 relationship outcomes | — |
| R-02 | PARTIAL (field NOT_IMPLEMENTED; protective invariant VERIFIED) | repo-wide grep, zero `intraday_monetisability_advisory` hits in active code | executed | The advisory field itself doesn't exist yet (design text only). The narrower invariant it would need (final_action never independently computed downstream) does hold. |
| R-03 | PARTIAL | `CanonicalOptionChainService._quote_as_of` (v1-compatible); `CHAIN_V2_COLUMNS` (missing `first_traded_utc`) | executed — v1 reader compatible; v2 writer emits everything except `first_traded_utc`; all 5 fixtures classified | §8.1 requires `first_traded_utc` when supplied; never populated by any writer. No dedicated missing-field fixture exists (confirms preflight/F-03). Legacy v1 store has no pre-write conflict check (separate, pre-existing finding, not new). |
| R-04 | PARTIAL | `scripts/avshunter_options_intelligence.py:7982-7995` (v1/v2 branch selection, source-cited not executed live) | executed for the flag-override mechanism (no production config touched) and non-destructiveness scan | Full live reader-switch end-to-end is BLOCKED/code-read-only under the no-live-call rule — cannot be executed without running the acquisition pipeline. |
| R-05 | FAILED | `intelligence-lab/static/index.html` (text-read) | executed field-presence checks | l2-size-hiding PASS. `underlying_nbbo_*` fields absent from the UI entirely (only `underlying_nbbo_mid`/`underlying_last` wired). Table view shows none of the 5 designated inline fields (they exist only in the modal). Empty-value fallback uses non-canonical tokens (`NOT_OBSERVED` etc.), never the 4 required (`MISSING`/`STALE`/`NOT_APPLICABLE`/`INSUFFICIENT_DATA`). |
| R-06 | PARTIAL | `contracts/interpreter_handoff_materializer.py` (defines `lab_signal_book_v3`) vs. `contracts/lab_control.py`/`intelligence-lab/intelligence_lab.py` (still stamp `lab_signal_book_v2`) | executed | §11 says "the governed book becomes `lab_signal_book_v3`" — in practice two differently-versioned books coexist; v3 is a derived handoff-only artefact, not the Lab's own live book. UI field-naming governance itself is correct. |
| R-07 | FAILED | 10 named suite groups, real pytest subprocess execution, junitxml-verified | 284 tests run, 6 failures across 4 groups (lab materialisation, pipeline interpreter handoff guard, CDS/registry, morning/EOD fixtures) | New failures relative to the implementer's claimed 116/0 (and later per-phase) baselines, not disclosed in the implementation record: `test_priority_rank_byte_identical_to_frozen_baseline` (see `20_design_inconsistencies.md` — implementer's "unrelated fixture drift" claim is UNVALIDATED, not confirmed); 2× `test_morning_handoff_finalizer.py` (`MATERIALIZER_MISSING_IDENTITY`); 2× `test_pipeline_interpreter_trusted_source.py` (path-resolution regression); 1× `test_cds2_historical_prices.py` (likely subprocess/PYTHONPATH environment issue, reported not waved away). |
| R-08 | VERIFIED | `contracts/options_liquidity_lifecycle.py::MATURATION_SCORE_AUTHORITY`; `market_structure/` (zero `vanguard` imports) | executed | — |
| R-09 | VERIFIED | exhaustive AST scan, all active directories | executed, zero violations | — |

## Section 7 — Run-artefact verification

**Entirely precondition-failed.** No MSI-active run exists anywhere on disk (confirmed in preflight and independently reconfirmed by the regression agent). Every §20 criterion requiring live-run evidence (4, 5, 7, 9's live portion, 10, 13, 14's live portion, and every §15 run-summary metric) is BLOCKED. Criteria answerable offline are cross-referenced to the covering test ID: criterion 2 → PASS (R-05's l2-hiding sub-check), criterion 8 → PASS (R-01/R-08), criterion 17 → PASS (R-09), criterion 12 → FAIL (R-07). See `audit/msi/07_run_artefacts.md` for the full 17-row table with evidence citations.
